//! Bare electrolyte between two electrodes, small applied current.
//! Sanity check: should produce a near-instantaneous ohmic step (R0 only).

pub fn run() {
    todo!(
        "spawn balanced cation/anion pairs in a domain, apply small galvanostatic \
         current, write CSV, eyeball that V(t) is a flat ohmic plateau"
    )
}
